@{
    Title = 'Deep'
    Items = @(
        @{
            Label = 'L1'
            Children = @(
                @{
                    Label = 'L2'
                    Children = @(
                        @{ Label = 'L3' }
                    )
                }
            )
        }
    )
}
